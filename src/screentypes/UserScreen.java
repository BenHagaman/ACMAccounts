package screentypes;

import screentypes.extensibility.UserScreenLevel;

public enum UserScreen implements UserScreenLevel{
	PRODUCT_SELECTION_SCREEN,
	ACCOUNT_OPTIONS_SCREEN,
	ACCOUNT_HISTORY_SCREEN,
	ADD_MONEY_SCREEN
}
